@import "~style/common.styl"

.option-with-switch-container
  display flex
  justify-content space-between
  align-items center
  margin-top rem(3)
  padding rem(10)
  height 55px
  width 100%
  background-color $grey_lightest
  border-radius 3px